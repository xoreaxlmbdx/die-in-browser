# Example

This folder shows how to embed and use `diec` directly in the browser via the
`DIEInBrowser` API from `die.js`.

## 📁 Required Files

To run this example locally, your directory must contain the following files:

- `libv86.js`
- `die.js`
- `v86.wasm`
- `seabios.bin`
- `vgabios.bin`
- `buildroot-state.bin.zst`

> 💡 These files are placed in the same directory as `index.html`.

## ▶️ How to Run

The easiest way to run this example is to serve it with a local HTTP server:

```bash
npx serve
```
